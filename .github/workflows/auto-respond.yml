name: PR Comment

on:
  pull_request:
    types: [opened]

jobs:
  comment:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Comment on PR
        run: |
          gh issue comment --repo ${{ github.repository }} --issue ${{ github.event.number }} --body "🚀 Thanks for your PR!🎓 I'm currently preparing for exams and won't be able to review pull requests until **March 10, 2025**. You have been put in a review queue. ✅"
