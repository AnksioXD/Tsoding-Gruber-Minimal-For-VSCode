name: Auto-Respond to PRs
on:
  workflow_dispatch:
  pull_request:
    types: [opened]
jobs:
  respond:
    runs-on: ubuntu-latest
    steps:
      - name: Comment on new PRs
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.payload.pull_request.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: "🚀 Thanks for your PR!🎓 I'm currently preparing for exams and won't be able to review pull requests until **March 10, 2025**. You have been put in a review queue. ✅"
            });
